open_cmd: xdg-open

shell:
  command: /bin/bash
  args:
    - --login

tmux:
  shell: /bin/bash

fontconfig:
  enabled: false
  font_dir: "{{ ansible_user_dir }}/.local/share/fonts"

alacritty:
  font_size: 12

commands:
  rm:
    params: "-I"
  exa:
    params: "-lh --group-directories-first"

desktop:
  enabled: false
