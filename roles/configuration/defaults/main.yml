open_cmd: xdg-open
shell: /bin/bash
fontconfig: true
distrobox: true

font_dir: "{{ ansible_user_dir }}/.local/share/fonts"

alacritty:
  font_size: 9

commands:
  ls:
    params: "-I"
  exa:
    params: "-lh --group-directories-first"

desktop:
  enabled: true
