- name: Place solaar configuration
  copy:
    src: "{{ role_path }}/files/solaar"
    # dest: "{{ ansible_facts.user_dir }}/.var/app/io.github.pwr_solaar.solaar/config"
    dest: "{{ ansible_facts.user_dir }}/.config"
    directory_mode: 0700
    mode: 0600
